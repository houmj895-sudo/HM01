# 完整的工作空间结构
control_ws/
├── config/
│   ├── ethercat_config.yaml        # EtherCAT 通信参数（如周期、接口名）
│   ├── motion_config.yaml          # 每个轴的参数（分辨率、减速比、限位等）
│   └── log_config.yaml             # 日志策略（可选）
└── src/
    ├── SOEM/                       # ✅ SOEM 原始库（保持原样）
    └── soem_ros2_driver/
        ├── CMakeLists.txt
        ├── package.xml
        ├── include/
        │   └── l6n_soem_csp/
        │       ├── axis.hpp              # 🧩 单轴类定义
        │       ├── motion_manager.hpp    # 🧩 多轴管理器
        │       ├── cia402_fsm.hpp        # 状态机
        │       ├── common_defs.hpp       # 通用常量/宏定义
        │       ├── logger.hpp            # 日志封装
        │       └── soem_iface.hpp        # SOEM 接口层
        ├── src/
        │   └── l6n_soem_csp/
        │       ├── axis.cpp
        │       ├── motion_manager.cpp
        │       ├── cia402_fsm.cpp
        │       ├── logger.cpp
        │       ├── soem_iface.cpp
        │       ├── slave_scan_node.cpp   # 🧪 用于通信验证的扫描节点
        │       └── csp_node.cpp          # 🚀 主节点（多轴CSP控制）
        ├── lib/
        │   └── libsoem_iface.a           # ✅ （可选）封装后静态库
        ├── launch/
        │   └── csp_launch.py             # ROS2 启动文件（后期添加）
        └── README.md
