好的。我们先把虚拟环境**创建出来**，再激活。按顺序执行下面几行（一次粘贴一行）：

### 步骤 1：创建目录并进入

```bash
mkdir -p ~/HM01
cd ~/HM01
```

### 步骤 2：确保有 venv 模块（若已安装会提示已是最新）

```bash
sudo apt update
sudo apt install -y python3-venv
```

### 步骤 3：创建并激活虚拟环境

```bash
python3 -m venv venv
source venv/bin/activate
```

