
# 执行虚拟环境压缩打包（可选但推荐）：

```bash
tar -czvf venv_backup.tar.gz venv/
```

这会生成一个 `venv_backup.tar.gz` 文件，大约 3~5 GB，可以放在项目根目录或移动硬盘。
之后任何时候只需：

```bash
tar -xzvf venv_backup.tar.gz
source venv/bin/activate
```

即可恢复到当前状态。

---

